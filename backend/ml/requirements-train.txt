# Training-only dependencies (NOT needed by the FastAPI runtime).
# Install in a separate env / Colab; the backend only needs onnxruntime.
#
#   python -m venv .venv-train && .venv-train/Scripts/activate   # (Windows)
#   pip install -r requirements-train.txt
#
# For CUDA, install the matching torch build from https://pytorch.org instead.
torch>=2.2
torchvision>=0.17
onnx>=1.16
onnxruntime>=1.18
pandas>=2.0
openpyxl>=3.1          # read the killa92 dataset .xlsx label files
numpy>=1.26
pillow>=10.0
scipy>=1.11            # Pearson/Spearman in eval.py
kaggle>=1.6           # optional: dataset download CLI
